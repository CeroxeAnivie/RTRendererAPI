package top.ceroxe.rt.renderer.api;

/** Arithmetic operation combining scaled source and destination attachment values. */
public enum BlendOperation {
    ADD,
    SUBTRACT,
    REVERSE_SUBTRACT,
    MINIMUM,
    MAXIMUM
}
