# 安全策略

当前受支持版本为 `0.1.x`。请不要在公开 issue 中披露可被利用的 native handle、越界、
着色器输入验证或供应链问题；请通过 GitHub 仓库的私密安全报告功能提交，并附最小复现、
影响版本、驱动与系统信息。维护者确认后会给出修复与披露时间表。

API 不信任外部 native handle 或场景尺寸。安全问题修复必须保留输入上限、所有权状态机、
Vulkan 返回码分类和异常路径资源回收，不接受仅隐藏崩溃的降级处理。
